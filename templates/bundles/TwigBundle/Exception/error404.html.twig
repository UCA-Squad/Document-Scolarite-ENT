{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid min-vh-100 d-flex align-items-center justify-content-center bg-light">
        <div class="card shadow-sm rounded-3 w-100" style="max-width:720px;">
            <div class="card-body text-center p-4">
                <h1 class="fw-bold mb-1"> Erreur </h1>

                <h5 class="text-muted mb-3">
                    Page introuvable
                </h5>

                <p class="text-secondary mb-4">La page demandée n'a pas pu être affichée.</p>

                <div>
                    <a href="{{ path('scola', {'route': ''}) }}" class="btn btn-primary btn-lg">Accueil</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
